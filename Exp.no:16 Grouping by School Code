import pandas as pd

data = {
    'school_code': ['s001', 's002', 's001', 's002', 's003'],
    'name': ['Ram', 'Sam', 'Ravi', 'Kumar', 'John'],
    'age': [15, 16, 14, 15, 17]
}

df = pd.DataFrame(data)

grouped = df.groupby('school_code')

print(grouped)
print(type(grouped))
